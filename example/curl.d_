#include "curl/multi.h"

void main() {
    CURLM *cm;
    CURLMsg *msg;

    curl_global_init(CURL_GLOBAL_ALL);

    cm = curl_multi_init();

    enum MAX = 10L;
    curl_multi_setopt(cm, CURLMoption.CURLMOPT_MAXCONNECTS, MAX);
}
